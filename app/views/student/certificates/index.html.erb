<%= render "shared/page_header", title: t("academic.certificates.student") %><div class="grid gap-4 sm:grid-cols-2"><% @certificates.each do |certificate| %><article class="ds-card p-5"><h2 class="font-bold"><%= link_to t("academic.certificate_types.#{certificate.certificate_type}"), student_certificate_path(certificate), class: "text-app-primary" %></h2><p class="ds-caption"><%= l(certificate.issued_on) %></p></article><% end %></div>
