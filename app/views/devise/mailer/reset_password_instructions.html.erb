<h1><%= t("authentication.mailer.reset.title") %></h1>
<p><%= t("authentication.mailer.reset.greeting", name: @resource.full_name) %></p>
<p><%= t("authentication.mailer.reset.instructions") %></p>
<p><%= link_to t("authentication.mailer.reset.action"), edit_password_url(@resource, reset_password_token: @token) %></p>
<p><%= t("authentication.mailer.reset.ignore") %></p>
<p><%= t("authentication.mailer.reset.expiry") %></p>
