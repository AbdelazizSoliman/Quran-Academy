<%= render "shared/page_header", title: t("academic.exams.teacher") %><div class="grid gap-4"><% @exam_sessions.each do |exam| %><article class="ds-card p-5"><h2 class="font-bold"><%= link_to exam.title, teacher_exam_path(exam), class: "text-app-primary" %></h2><p class="ds-caption"><%= l(exam.starts_at, format: :long) %> · <%= t("academic.exam_statuses.#{exam.status}") %></p></article><% end %></div>
