<%= render "shared/page_header", title: I18n.locale == :ar ? @lesson.title_ar : @lesson.title_en, description: @lesson.public_id, badge: scheduling_status_badge(@lesson) %><%= render "shared/components/card", title: t("scheduling.sections.lesson") do %><p dir="ltr"><%= scheduling_time(@lesson.starts_at, zone: ActiveSupport::TimeZone[@lesson.academy_time_zone]) %></p><p class="mt-3"><%= @lesson.course_offering.title_en %></p><% end %>
