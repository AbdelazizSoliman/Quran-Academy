<%= render "shared/page_header", title: t("scheduling.teacher.availability"), primary_action: render("shared/components/button", label: t("scheduling.actions.new_availability"), href: new_teacher_availability_path) %><div class="grid gap-3"><% @availabilities.each do |availability| %><%= link_to edit_teacher_availability_path(availability), class: "ds-card p-4" do %><%= t("scheduling.catalogs.weekdays.#{availability.weekday}") %> · <span dir="ltr"><%= l(availability.starts_at_local, format: :time) %>–<%= l(availability.ends_at_local, format: :time) %></span><% end %><% end %></div>
