<%= render "shared/page_header", title: t("scheduling.actions.edit_availability") %><%= render "admin/teacher_availabilities/form", availability: @availability, teachers: [current_user.teacher_profile], form_model: @availability, form_url: teacher_availability_path(@availability), cancel_url: teacher_availabilities_path %>
