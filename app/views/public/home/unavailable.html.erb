<% content_for :title, t("public.unavailable.page_title", academy: @public_site.academy_name) %>
<section class="grid min-h-[65vh] place-items-center px-4 py-20 text-center">
  <div class="max-w-xl">
    <div class="mx-auto grid size-16 place-items-center rounded-2xl bg-emerald-50 text-2xl text-emerald-900" aria-hidden="true">خ</div>
    <h1 class="mt-6 text-3xl font-bold text-emerald-950"><%= t("public.unavailable.title") %></h1>
    <p class="mt-4 leading-7 text-slate-600"><%= t("public.unavailable.description") %></p>
    <%= link_to t("public.navigation.login"), new_user_session_path(locale: @public_site.locale),
                class: "mt-7 inline-flex rounded-xl bg-[var(--public-primary)] px-5 py-3 font-bold text-white" %>
  </div>
</section>
